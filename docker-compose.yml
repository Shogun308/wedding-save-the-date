version: '3.8'

services:
  wedding-save-the-date:
    build: .
    container_name: wedding-save-the-date
    restart: unless-stopped
    ports:
      - "10445:8080"
    environment:
      - PORT=8080
      - NODE_ENV=production
